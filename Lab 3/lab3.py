import copy  # Модуль для створення копій об'єктів (поверхневої та глибокої)

# === Клас фігури
class Square:
    def __init__(self, side, color):  # Конструктор класу, приймає сторону і колір
        self.side = side  # Зберігає довжину сторони як властивість об'єкта
        self.color = color  # Зберігає колір як властивість об'єкта

    def area(self):  # Метод для обчислення площі квадрата
        return self.side ** 2  # Площа = сторона у квадраті

    def display(self):  # Метод для виводу інформації про квадрат
        print(f"Квадрат: сторона = {self.side}, колір = {self.color}, площа = {self.area()}")  # Виводить дані про квадрат

    def clone(self):  # Метод для створення копії об'єкта
        return copy.deepcopy(self)  # Повертає глибоку копію об'єкта (незалежну від оригіналу)

# === Консольна логіка
def main():
    print("== Створення базового шаблону квадрата ==")  # Повідомлення користувачу

    side = float(input("Введіть довжину сторони: "))  # Зчитує сторону від користувача і перетворює на число
    color = input("Введіть колір: ")  # Зчитує колір

    prototype_square = Square(side, color)  # Створює базовий квадрат-прототип

    shapes = [prototype_square]  # Створює список з фігурами, першим додає прототип

    while True:  # Нескінченний цикл, поки користувач не вибере вихід
        print("\n1. Клонувати квадрат")  # Меню: пункт 1
        print("2. Показати всі квадрати")  # Меню: пункт 2
        print("0. Вихід")  # Меню: вихід

        choice = input("Ваш вибір: ")  # Зчитує вибір користувача

        if choice == '1':  # Якщо обрано клонування
            new_square = prototype_square.clone()  # Копіює прототип через метод clone()

            # Запит змін властивостей у користувача
            new_side = input("Нова довжина сторони (Enter — без змін): ")  # Запит нової сторони
            new_color = input("Новий колір (Enter — без змін): ")  # Запит нового кольору

            if new_side:  # Якщо введено значення
                new_square.side = float(new_side)  # Змінює сторону на нову
            if new_color:  # Якщо введено значення
                new_square.color = new_color  # Змінює колір на новий

            shapes.append(new_square)  # Додає новий квадрат у список
            print("✅ Квадрат скопійовано.")  # Повідомлення про успішне клонування

        elif choice == '2':  # Якщо обрано перегляд списку
            print("\n📦 Список квадратів:")  # Заголовок
            for i, sq in enumerate(shapes, 1):  # Перебирає всі квадрати у списку з індексацією
                print(f"{i}. ", end="")  # Виводить номер квадрата
                sq.display()  # Викликає метод display для виводу даних про квадрат

        elif choice == '0':  # Якщо обрано вихід
            break  # Вихід з циклу

        else:  # Якщо обрано невідому опцію
            print("❗ Невідомий вибір!")  # Повідомлення про помилковий ввід

# Запуск програми, якщо файл виконується напряму
if __name__ == "__main__":
    main()  # Викликає головну функцію
